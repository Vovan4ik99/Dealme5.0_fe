import styles from './CustomDivider.module.scss';

const CustomDivider = () => {

	return (
		<div className={styles['divider']}>
			<span className={styles['divider__text']}>lub</span>
		</div>
	);
}

export default CustomDivider;